{
  "name": "sap-mcp-bridge-vendor",
  "version": "1.0.0",
  "private": true,
  "description": "The MCP server and its dependencies, pinned exactly as shipped. Installed with npm ci.",
  "license": "MIT",
  "dependencies": {
    "abap-adt-mcp": "2.1.0"
  }
}
